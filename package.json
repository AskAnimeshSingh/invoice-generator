{
  "name": "invoice-generator",
  "version": "1.0.0",
  "private": true,
  "description": "React invoice generator with browser PDF and localStorage signature",
  "scripts": {
    "start": "npm run dev --prefix client",
    "dev": "npm start",
    "build": "npm run build --prefix client",
    "install:all": "npm install && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
